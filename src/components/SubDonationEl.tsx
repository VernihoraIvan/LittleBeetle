// import { productDescriptions } from "@/utilities/creatorsData";
// import SubDonation from "./SubDonation";

const SubDonationSection = () => {
  return (
    <>
      {/* {productDescriptions.map((product) => {
        return (
          <SubDonation
            title={product.title}
            description={product.description}
            imagePath={product.imagePath}
          />
        );
      })} */}
      {/* {productDescriptions.find(product) => product.name === "The book"} */}
    </>
  );
};

export default SubDonationSection;
